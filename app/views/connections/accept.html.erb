<h1>Tell Us 2 People to Introduce</h1>

<div class="errors">
	<% if @connection.errors.present? %>
		<% @connection.errors.each do |key, message| %>
			<p class="error"><b><%= key %></b>: <%= message %></p>
		<% end %>
	<% end %>
</div>

<h1>Accept View </h1>
